"use client";
import React from "react";
import { FiUser } from "react-icons/fi";

const ProfileIcon = () => {
  return (
    <div className="bg-red-secondary rounded-full p-1">
      <FiUser size={24} color="white" />
    </div>
  );
};

export default ProfileIcon;
